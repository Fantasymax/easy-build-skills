# 领域术语表

> **填法**：B6 ① 用户给的术语清单。**用户原话保留**——AI 可以补充解释，但词条本身不许改写。
>
> 这份术语表 AI 在执行 skill 时按需读，作为消除歧义的硬依据。

---

## 必用术语（按场景分类）

### 类别 1：{用户给的归类，如"项目管理"}

| 术语 | 一句话定义（用户原话） | 何时必须用 |
|---|---|---|
| {术语 1} | {用户解释} | {场景} |
| {术语 2} | {用户解释} | {场景} |

### 类别 2：{用户给的归类}

| 术语 | 一句话定义 | 何时必须用 |
|---|---|---|
| ... | ... | ... |

## 禁词与禁用句式（B6 ③）

> AI 输出时**绝不许**出现以下词或句式：

- ❌ {禁词 1} — 替代用 {替代词}
- ❌ {禁词 2} — 替代用 {替代词}
- ❌ {禁用句式} — 替代用 {替代句式}

## 公司 / 行业特定缩写（如适用）

| 缩写 | 全称 | 用法说明 |
|---|---|---|
| ... | ... | ... |

## 同义词处理规则

如果用户语料里某个概念有多个叫法（比如"客户/用户/甲方"），明确：
- **首选**：{用户首选词}
- **可接受**：{其它叫法}
- **绝不用**：{被排除的叫法}

---

## AI 使用指引

执行 skill 时：
1. 输入文本里出现"必用术语"列里的词 → **保持原样**，不要改成同义词
2. 输出时优先用"必用术语"列里的词，不要用"禁词"列里的词
3. 拿不准时停下来问用户："你希望这里用 X 还是 Y？"
